<%= content_tag(:div, class: "container align-items-center mt-5") do %>
    <%= content_tag(:div, class: "row align-items-center justify-content-center") do %>
        <%= content_tag(:div, class: "col-10 col-sm-8 col-md-6") do %>
            <h1>Events</h1>
            <table class="table">
                <thead>
                    <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Time</th>
                    <th scope="col">Location</th>
                    <th scope="col">Mandatory?</th>
                    <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                <!-- for loop this table row and dynamically fill in the content -->
                    <tr>
                    <td class="align-middle">Meeting 1</td>
                    <td class="align-middle">5/22/22 5:00 PM</td>
                    <td class="align-middle">MSC</td>
                    <td class="align-middle">Yes</td>
                    <td><button type="button" class="btn btn-primary">Description</button></td>
                    <!-- need to add button action to see description or link to events/id -->
                    </tr>
                </tbody>
                </table>
        <% end %>
    <% end %>
<% end %>